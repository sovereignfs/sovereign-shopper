{
  "name": "@sovereignfs/sovereign-shopper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "db:generate:sqlite": "drizzle-kit generate --schema db/schema.ts --out migrations/sqlite --dialect sqlite",
    "db:generate:pg": "drizzle-kit generate --schema db/schema.postgres.ts --out migrations/postgres --dialect postgresql"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@sovereignfs/sdk": "workspace:*",
    "@sovereignfs/ui": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@sovereignfs/tsconfig": "workspace:*",
    "@types/node": "^22.10.2",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:",
    "vitest": "^4.1.8"
  }
}
